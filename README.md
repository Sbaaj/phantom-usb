# phantom-usb

* A python-based filesystem watcher that unlocks a hidden **versacrypt** volume via a trigger file. *

The purpose of this tool is to reveal a hidden, encrypted section in your USB by creatining a file containing a password
